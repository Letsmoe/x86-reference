---
const request = await fetch("https://raw.githubusercontent.com/the-meta-lang/metax/main/install.sh");

const installSript = await request.text();

return new Response(installSript, {
	headers: {
		"Content-Type": "text/plain",
	},
});
---